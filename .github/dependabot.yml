# Dependabot configuration
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
#
# The previous file declared an empty package-ecosystem, which Dependabot
# rejects, so no version updates were ever produced. Two ecosystems matter
# here: the npm manifest under /app and the GitHub Actions used by CI.

version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/app"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 10
    groups:
      mantine:
        patterns:
          - "@mantine/*"
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react*"
          - "@vitejs/plugin-react"
      lint:
        patterns:
          - "eslint*"
          - "@eslint/*"
          - "globals"
      build:
        patterns:
          - "vite"
          - "vitest"
          - "postcss*"
    labels:
      - "dependencies"
    commit-message:
      prefix: "chore(deps)"
    ignore:
      # eslint-plugin-react (7.37.x) declares peer support up to ESLint 9.
      # Lift this once it supports 10, then bump eslint and @eslint/js together.
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@eslint/js"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    labels:
      - "dependencies"
      - "ci"
    commit-message:
      prefix: "chore(ci)"
